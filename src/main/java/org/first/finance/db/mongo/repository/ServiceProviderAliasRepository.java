/*
package org.first.finance.db.mongo.repository;

import org.first.finance.db.mongo.entity.ServiceProviderAlias;
import org.springframework.data.repository.CrudRepository;

import java.util.List;

public interface ServiceProviderAliasRepository extends CrudRepository<ServiceProviderAlias, String> {
    ServiceProviderAlias findByAlias(String alias);

    List<ServiceProviderAlias> findAllByMnemonicIsNotNull();

    ServiceProviderAlias findByMnemonicIsLikeIgnoreCase(String searchableString);
}
*/
